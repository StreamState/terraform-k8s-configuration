apiVersion: argoproj.io/v1alpha1
kind: EventBus
metadata:
  name: default
spec:
  nats:
    native:
      # Optional, defaults to 3. If it is < 3, set it to 3, that is the minimal requirement.
      replicas: 3
      # Optional, authen strategy, "none" or "token", defaults to "none"
      auth: token
#      containerTemplate:
#        resources:
#          requests:
#            cpu: "10m"
#      metricsContainerTemplate:
#        resources:
#          requests:
#            cpu: "10m"
#      antiAffinity: false
#      persistence:
#        storageClassName: standard
#        accessMode: ReadWriteOnce
#        volumeSize: 10Gi

---
apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: streamstatewebservice
  labels:
    eventsource: streamstate
spec:
  # replicas: 3 # needed for HA
  #selector:
  #  matchLabels:
  #    app: argobackend
  #service:  # TODO, get more robust service
  #  ports:
  #    - port: 12000
  #      targetPort: 12000
  webhook:
    # event-source can run multiple HTTP servers. Simply define a unique port to start a new HTTP server
    runcontainer:
      # port to run HTTP server on
      port: "12000"
      # endpoint to listen to
      endpoint: /build/container
      # HTTP request method to allow. In this case, only POST requests are accepted
      method: POST
    example:
      # port to run HTTP server on
      port: "12000"
      # endpoint to listen to
      endpoint: /build/example
      # HTTP request method to allow. In this case, only POST requests are accepted
      method: POST
      #authSecret:
      #  name: my-webhook-token
      #  key: my-token
#    example-foo:
#      port: "12000"
#      endpoint: /example2
#      method: POST

# Uncomment to use secure webhook
#    example-secure:
#      port: "13000"
#      endpoint: "/secure"
#      method: "POST"
#      # k8s secret that contains the cert
#      serverCertSecret:
#        name: my-secret
#        key: cert-key
#      # k8s secret that contains the private key
#      serverKeySecret:
#        name: my-secret
#        key: pk-key

# ---        
# apiVersion: v1
# kind: Service
# metadata:
#   name: argoeventsservice
#   #namespace: default
# spec:
#   type: NodePort
#   selector:
#     app: argobackend
#   ports:
#   - port: 80
#     targetPort: 12000
#     protocol: TCP
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: espv2
spec:
  backend:
    serviceName: espv2 # Name of the Service targeted by the Ingress
    servicePort: 80 # Should match the port used by the Service
---
apiVersion: v1
kind: Service
metadata:
  name: espv2
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: '{"default": "espv2"}'
spec:
  ports:
  - port: 80
    #targetPort: 12000 # this "worked" when port-forwarding but I think it byappsed esp
    targetPort: 8082 # try directly calling esp
    protocol: TCP
    name: http
  #selector:
  #  eventsource: streamstate
  type: ClusterIP 
  selector:
    controller: eventsource-controller
    eventsource-name: streamstatewebservice
    # owner-name: webhook
