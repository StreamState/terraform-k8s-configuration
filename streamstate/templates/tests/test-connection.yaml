apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streamstate.fullname" . }}-test-connection"
  labels:
    {{- include "streamstate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streamstate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
