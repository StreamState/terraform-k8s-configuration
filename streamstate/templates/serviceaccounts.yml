{{- range $k, $v := .Values.serviceaccounts }}
  apiVersion: v1
  kind: Namespace
  metadata:
    name: {{ .name }}
    namespace: {{ .namespace }}
    {{ if .iam_binding }}
    annotations:
      iam.gke.io/gcp-service-account: {{ .iam_binding }}
    {{ end }}
  ---
{{- end }}